
Building

To build you will need git, g++, GHC (at least 7.4.1), Cabal (at least 1.18) and some time to build dependencies.

cd simple-search
make

Using

./indexer ./docs index.inv


./searcher index.inv
